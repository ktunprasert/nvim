return {
    cmd_env = {
        -- this is a fish array so single arg command is better than a table
        -- e.g. fish_lsp_diagnostic_disable_error_codes = "2002 1001",
        fish_lsp_diagnostic_disable_error_codes = "2002",
    },
}
